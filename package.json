{
  "name": "easy-email",
  "scripts": {
    "install-all": "lerna exec -- yarn",
    "build": "lerna exec -- yarn build",
    "dev": "cd demo && yarn dev",
    "demo": "cd demo && yarn build"
  },
  "dependencies": {
    "browser-image-compression": "^1.0.17",
    "lerna": "^4.0.0",
    "vite": "^2.6.14"
  }
}
